#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    int T = 1;
    // if (!(cin >> T) || T <= 0) {
    //     cerr << "Error: Invalid number of test cases";
    //     return 1;
    // }

    while (T--) {
        int N;
        if (!(cin >> N) || N <= 0) {
            cerr << "Error: Invalid values of N ";
            return 1;
        }
        int K;
        if (!(cin >> K) || K <= 0) {
            cerr << "Error: Invalid values of N ";
            return 1;
        }
    }
    return 0;
}
